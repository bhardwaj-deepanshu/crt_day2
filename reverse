#include <stdio.h>
int main()
{
    int rev = 0, n, i;
    printf("please enter a no for reverse:");
    scanf("%d",&n);
    while(n)
    {
        i = n % 10;
        rev = rev * 10 + i;
        n /= 10;
    }
    printf("%d",rev);
    return 0;
}
